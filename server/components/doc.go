// Package components acts as the dependency injection container (composition root).
// It wires together config, logging, infrastructure adapters, repositories,
// and application services into ready-to-use components.
package components
