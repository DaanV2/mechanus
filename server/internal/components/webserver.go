package components

import "github.com/DaanV2/mechanus/server/pkg/servers"

func WebServer(staticFolder string) (*servers.Manager, error) {
	// TODO built web servers, grpc, etc etc
	return &servers.Manager{}, nil
}
