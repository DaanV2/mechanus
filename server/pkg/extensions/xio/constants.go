package xio

const (
	// DEFAULT_DIR_PERMISSIONS is the default permission set for directories (rwxr-x---).
	DEFAULT_DIR_PERMISSIONS = 0750
	// DEFAULT_FILE_PERMISSIONS is the default permission set for files (rw-------).
	DEFAULT_FILE_PERMISSIONS = 0600
)
