// Package application orchestrates use cases.
// It coordinates domain logic (engine) with interfaces (ports).
// Implements flows like user login, registration, and system checks.
package application
